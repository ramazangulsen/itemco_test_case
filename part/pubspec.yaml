name: itemco_test_case_part
description: Views for white label.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4

  itemco_test_case:
    path: ../
